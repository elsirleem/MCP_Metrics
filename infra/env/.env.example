# Required secrets
GITHUB_PAT=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Database (if running api outside docker-compose)
DATABASE_URL=postgres://mcp:mcp_password@localhost:5432/mcp_metrics

# MCP server
MCP_BASE_URL=http://localhost:3001

# Ingestion job
INGEST_REPOSITORIES=owner/repo
INGEST_LOOKBACK_DAYS=1
